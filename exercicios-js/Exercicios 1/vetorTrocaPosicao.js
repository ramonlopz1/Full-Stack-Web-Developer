const vetorTroca = (array1 = [], array2 = []) => {
    
    array1.forEach(
        function(num, index) {
            array2.replace(array2[i], array1[i])
        }
    )

    console.log(array1, array2)
}

vetorTroca([3,4,5,6], [2,2,3,5])